import { approvedPart1 } from "./approved-master-part-1";

// Use one valid raster image. Do not concatenate separately encoded JPEG payloads.
export const PAINTED_MOCKUP = `data:image/jpeg;base64,${approvedPart1}`;
